import styler from './styler';
export {ThemeProvider} from './context/Context';
export {useColors} from './context/Context';
export {useStyler} from './styler';

const ActivityIndicator = styler.ActivityIndicator;
const Button = styler.Button;
const FlatList = styler.FlatList;
const Image = styler.Image;
const ImageBackground = styler.ImageBackground;
const KeyboardAvoidingView = styler.KeyboardAvoidingView;
const Modal = styler.Modal;
const Pressable = styler.Pressable;
const RefreshControl = styler.RefreshControl;
const ScrollView = styler.ScrollView;
const SectionList = styler.SectionList;
const StatusBar = styler.StatusBar;
const Switch = styler.Switch;
const Text = styler.Text;
const TextInput = styler.TextInput;
const TouchableHighlight = styler.TouchableHighlight;
const TouchableOpacity = styler.TouchableOpacity;
const TouchableWithoutFeedback = styler.TouchableWithoutFeedback;
const View = styler.View;
const VirtualizedList = styler.VirtualizedList;

export {
  ActivityIndicator,
  Button,
  FlatList,
  Image,
  ImageBackground,
  KeyboardAvoidingView,
  Modal,
  Pressable,
  RefreshControl,
  ScrollView,
  SectionList,
  StatusBar,
  Switch,
  Text,
  TextInput,
  TouchableHighlight,
  TouchableOpacity,
  TouchableWithoutFeedback,
  View,
  VirtualizedList,
}

export default styler;
